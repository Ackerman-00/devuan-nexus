--- a/Cargo.toml
+++ b/Cargo.toml
@@ -200,7 +200,7 @@
 # Disable any features that pull in systemd
 [features]
-default = ["font-kit", "wayland", "x11", "windows-manifest"]
+default = ["font-kit", "wayland", "x11"]
 
 # Remove systemd from optional dependencies if present
 [target.'cfg(target_os = "linux")'.dependencies]
-systemd = { version = "0.10", optional = true }
+# systemd removed for Devuan compatibility
 
--- a/crates/gpui/src/platform/linux/platform.rs
+++ b/crates/gpui/src/platform/linux/platform.rs
@@ -45,7 +45,7 @@
 
 #[cfg(target_os = "linux")]
 use {
-    zbus::{blocking::Connection, proxy},
+    zbus::blocking::Connection,
     anyhow::Context,
     futures::channel::oneshot,
 };
 
@@ -120,10 +120,12 @@
 
 #[cfg(target_os = "linux")]
 impl Platform for LinuxPlatform {
     fn new() -> Result<Self> {
-        // Try systemd-logind first, fallback to elogind D-Bus interface
-        let conn = Connection::system()
+        // Use system D-Bus (works with elogind, seatd, or plain dbus)
+        // Do not require systemd-specific interfaces
+        let conn = Connection::session()
             .or_else(|_| Connection::system())
             .context("Failed to connect to D-Bus")?;
+        
         Ok(Self { conn: Some(conn) })
     }
 }
